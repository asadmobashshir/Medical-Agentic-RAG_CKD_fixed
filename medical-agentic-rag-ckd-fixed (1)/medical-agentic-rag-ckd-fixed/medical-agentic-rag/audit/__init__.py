"""Audit trail package.

Named ``audit`` rather than ``logging`` so it does not shadow the standard
library ``logging`` module for absolute imports within this project.

Import submodules directly::

    from audit.audit_logger import AuditLogger
"""
