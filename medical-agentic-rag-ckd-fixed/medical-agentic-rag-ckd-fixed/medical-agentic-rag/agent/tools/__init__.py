"""Tool implementations and the registry that gates their execution.

Import submodules directly (see :mod:`agent.__init__` for the rationale)::

    from agent.tools.tool_registry import build_default_registry
"""
